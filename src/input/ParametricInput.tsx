/**
 * Input element for Parametric functions (where x and y depend on a parameter t).
 *
 * @param id the id of the input
 * @constructor
 */
export function ParametricInput({ id }: { id: number }) {
    return <></>;
}